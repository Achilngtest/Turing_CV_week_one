使用pandas与matplotlib处理`附件.xlsx`中的数据，分别完成如下需求：
1. 画出**5**张散点图，分别展示每个车队的`在途时长`，`自有变动成本`，`外部承运商成本`。
2. 在控制台中打印每个车队各项数据数据的`最大值`，`最小值`，`平均值`，`中位数`
3. 分别计算每个车队的最佳时均成本，并打印到控制台中，计算方法：
    - 计算 `总成本 = 自有变动成本 + 外部承运商成本`
    - 计算 `时均成本 = 总成本 / 在途时长`
4. 将各车队的最佳时均成本绘制为柱状图，以比较不同车队之间的成本表现