使用matplotlab库绘制函数 y=sin(x) 的图像，并同时满足以下所有要求：
- 曲线线型为虚线；
- y 轴范围固定为 ([-1.5,,1.5])；
- x 轴范围为 ([0,10])；
- 坐标轴标签分别为：x 轴 “x”，y 轴 “y”；
- 图表标题为 “三角函数”（注意解决中文乱码问题）；
- 显示网格线；
- 给曲线添加图例（标签为 `sin(x)`），并显示在图中合适位置。

**输出：** 一张满足以上全部要求的 Matplotlib 图像。

输出示例见`示例图片.png`